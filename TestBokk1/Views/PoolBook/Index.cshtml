@model IEnumerable<TestBokk1.Models.PoolBook>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Owner)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.HandTo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.BookID)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Owner)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.HandTo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.BookID)
        </td>
        <td>
            @Html.ActionLink(" Yet Edit", "Yet Edit", new { id=item.ID }) |
           
            @Html.ActionLink("Yet Delete", "Yet Delete", new { id=item.ID })
        </td>
    </tr>
}

</table>
